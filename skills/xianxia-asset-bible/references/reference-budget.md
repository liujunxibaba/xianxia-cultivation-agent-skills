# 参考图映射与素材预算

每个生成单元只选直接影响当前画面的参考图。人物、服装、场景、法宝、首帧、位置图和风格图全部计入预算；目标平台未明确时采用保守上限 9 张。

优先级：核心人物身份 > 当前服装 > 关键场景 > 唯一法宝 > 首帧/位置 > 法术风格 > 泛风格图。超限时合并角色设定卡、把次要外观转成文字、拆分生成单元。

素材声明必须写“参考什么、不继承什么”。人物图默认不继承姿态和背景；场景图默认不增加图中无关人物；法宝图只代表单一实例。

验收：每个 `@图片N` 都有唯一用途；提示词中没有未定义编号；同一法宝不会因多张参考图生成多个实例。
