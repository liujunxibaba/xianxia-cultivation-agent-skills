# 跨平台接入指南

本项目把“创作能力”和“平台接入”分离：`SKILL.md` 与 `references/` 构成平台无关核心；平台目录、UI 元数据、自动触发和知识库配置属于适配层。

## 能力要求

目标环境满足以下任意一种条件即可使用：

1. 支持文件夹式 Skills；
2. 支持项目规则、系统提示词或自定义 Agent；
3. 支持上传 Markdown/TXT 文件；
4. 至少允许把 Markdown 内容粘贴进上下文。

## 接入映射

| 本仓库内容 | 原生 Skill 平台 | 项目规则 / 自定义 Agent | 普通聊天 |
| --- | --- | --- | --- |
| `SKILL.md` | Skill 入口 | 主指令或项目规则 | 首先上传或粘贴 |
| `references/` | 按需读取资源 | 知识文件 | 按任务上传或粘贴 |
| `agents/openai.yaml` | Codex/OpenAI 可选 UI 元数据 | 忽略 | 忽略 |
| Skill 名称 | 自动或显式触发 | 写入 Agent 名称/描述 | 在请求中显式点名 |

## Codex

把完整 Skill 目录放入当前 Codex 环境识别的 Skills 目录。保留 `agents/openai.yaml` 可提供显示名称、简介和默认提示词等 UI 信息。

## Kimi、Qoder 与类似环境

不同版本和产品形态的入口名称可能是“自定义智能体”“项目规则”“Rules”“知识库”或“上下文文件”。不要依赖固定按钮名称，按以下语义映射：

- 主指令：使用 `SKILL.md`；
- 知识文件：使用 `references/`；
- 默认开场白：使用 README 中的通用启动指令；
- 自动触发不可用：在请求里显式写出 Skill 名称。

## 通用启动指令

```text
你将使用随附的 <skill-name> 文件夹完成任务。
先读取 SKILL.md，把它作为本任务的最高优先级工作流；
再按 SKILL.md 的要求读取 references/ 中与当前任务有关的文件。
忽略仅属于其他平台的 UI 元数据文件。
不要虚构未提供的资料，不要跳过输入核对和输出检查。

我的任务：<在这里填写任务>
```

## 维护规则

- 领域方法只维护在 `SKILL.md` 和 `references/`，不要为不同平台复制多套正文；
- 平台适配文件不得改变核心工作流或输出标准；
- 新增平台时优先补充接入映射，不把平台命令写进核心 Skill；
- 无法访问引用文件的平台，由用户明确上传相关文件；
- 不宣称所有平台具有相同的自动触发、文件读取或上下文容量。

